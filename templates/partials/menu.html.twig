<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">The Simple Bar/beers</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
		<a class="nav-link {% if route_name and route_name == 'home' %}active{% endif %}" href="{{ path('home') }}">Home</a>
        </li>
         {% for category in categories %}
        <li class="nav-item">
          <a class="nav-link {% if category_id and category_id == category.id %}active{% endif %}" href="{{ path('show_beer_category', { id : category.id}) }}">{{category.name}}</a>
        </li>
        {% endfor %}
        <li class="nav-item">
        {% if app.user %}
        <a class="nav-link {% if route_name and route_name == 'app_logout' %}active{% endif %}" href="{{ path('app_logout') }}">Logout</a>
        {% else %}
		<a class="nav-link {% if route_name and route_name == 'app_login' %}active{% endif %}" href="{{ path('app_login') }}">Login</a>
      {% endif %}
        </li>
        <li class="nav-item">
        {% if not app.user %}
		<a class="nav-link {% if route_name and route_name == 'app_register' %}active{% endif %}" href="{{ path('app_register') }}">Register</a>
    {% endif %}
        </li>
      </ul>
    </div>
  </div>
</nav>